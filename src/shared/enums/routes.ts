export enum auth {
    base = '/auth',
}

export enum main {
    base = '/main',
    info = 'info',
    chat_messages = 'chat_messages',
    favorites = 'favorites',
    calendar = 'calendar',
    tasks = 'tasks',
}
