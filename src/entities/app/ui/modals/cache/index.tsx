import React from 'react';

import styles from './styles.module.scss';

type Props = {};

function CacheView(props: Props) {
    // const { } = props;

    return <div className={styles.wrapper}>CacheView</div>;
}

export default CacheView;
