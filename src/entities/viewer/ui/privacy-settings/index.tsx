import React from 'react';

import { BaseTypes } from 'shared/types';

import { Viewer } from '../../model/types';

type Props = {} & BaseTypes.Statuses;

function PrivacySettingsView(props: Props) {
    // const { viewer, logoutClick, replaceAvatarClick, loading } = props;

    return <div>PrivacySettingsView</div>;
}

export default PrivacySettingsView;
