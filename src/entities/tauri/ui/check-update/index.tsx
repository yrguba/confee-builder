import React from 'react';

type Props = {};

function CheckUpdateView(props: Props) {
    // const { chat, subtitle, showDate, showChecked, onClick } = props;

    return <div>CheckUpdateView</div>;
}

export default CheckUpdateView;
